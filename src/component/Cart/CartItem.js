import React from "react";
import DeleteIcon from "@material-ui/icons/Delete";

function CartItem() {
    const { id, title, img, price, total, count } = item;
    const { increment, decrement, removeItem } = value;
  return <div>cartitrm</div>;
}

export default CartItem;
